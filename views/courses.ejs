<!doctype html>
<html lang="en">
<%- include("partials/header.ejs") %>
<%- include("partials/nav.ejs") %>

<body>
<div class="container">
    <%
    let buttons = [` <a class="" data-toggle="modal" href="#newClass"><i class="fas fa-folder-plus"></i>&nbsp;New Course</a>&nbsp;`,
        ` <a href="/assignments/new"><i class="fas fa-plus"></i>&nbsp;New Assignment</a>`]
    %>
    <%- include("partials/title.ejs", {title: "Courses", subtitle: `Instructor ${ user.firstname } ${ user.lastname }`,
    buttons: buttons, backName: "Home", backLink: "/"}) %>

    <div class="row">

        <div class="col-12">
            <% courses.forEach((course) => { %>
                <h3><%= course.name %>.<%= course.section %></h3>
                <div class="d-flex justify-content-between">
                    <span>
                        <strong>Join Code:</strong> <span
                                class="text-uppercase text-monospace"><%= course.code %></span>,
                        <strong>Enrolled Students:</strong> <%= course.students.length %>
                    </span>
                    <div class="">
                        <a class="" href="/courses/<%= course._id %>/manage"><i
                                    class="fas fa-users"></i> Manage Students</a>&nbsp;&nbsp;
                        <a class="text-danger" data-toggle="modal" href="#confirmDeleteCourse"><i
                                    class="fas fa-trash-alt"></i> Delete Course</a>
                    </div>
                </div>
                <div class="card pt-1 pb-1 p-3">
                    <h4>Assignments</h4>
                    <div class="table-responsive">
                        <table class="table table-bordered table-striped">
                            <thead>
                            <tr>
                                <th scope="col">Name</th>
                                <th scope="col" class="text-center" style="width: 1em;">Status</th>
                                <th scope="col">Due Date</th>
                                <th scope="col" nowrap>Late Due Date</th>
                                <th scope="col">Required Files</th>
                                <th scope="col">Tests</th>
                                <th scope="col" style="width: 1em;"><i class="fas fa-pencil-alt"></i></th>
                            </tr>
                            </thead>
                            <tbody>
                            <% course.assignments.forEach(function(assignment){ %>
                                <tr>
                                    <td nowrap><a href="/assignments/<%= assignment._id %>"><%= assignment.name %></a>
                                    </td>
                                    <td class="text-center" nowrap>
                                        <strong><%= (assignment.assigned) ? "Assigned" : "Draft" %></strong></td>
                                    <td nowrap><%= assignment.duedate %></td>
                                    <td><%= assignment.late %></td>
                                    <td><%= assignment.files.join(", "); %></td>
                                    <td><%- assignment.tests.map(test => `<a href="/test/${test._id}">${test.name}</a>`).join(", ") %></td>
                                    <td>
                                        <div class="dropdown text-center">
                                            <a class="" type="button" id="dropdownMenuButton" data-toggle="dropdown"
                                               aria-haspopup="true" aria-expanded="false">
                                                <i class="fas fa-ellipsis-v"></i>
                                            </a>
                                            <div class="dropdown-menu dropdown-menu-left"
                                                 aria-labelledby="dropdownMenuButton">
                                                <a class="dropdown-item"
                                                   href="/assignments/edit/<%= assignment._id %>/assign/<%= (assignment.assigned) ? "false" : "true" %>"><i
                                                            class="fas fa-external-link-alt"></i>&nbsp;<%= (assignment.assigned) ? "Unassign" : "Assign" %>
                                                </a>
                                                <a class="dropdown-item"
                                                   href="/assignments/grades/<%= assignment._id %>"><i
                                                            class="fas fa-user-edit"></i>&nbsp;Grades</a>
                                                <a class="dropdown-item" href="/assignments/<%= assignment._id %>"><i
                                                            class="fas fa-edit"></i>&nbsp;Edit Assignment</a>
                                                <a class="dropdown-item"
                                                   href="/assignments/<%= assignment._id %>/delete"><i
                                                            class="fas fa-trash-alt"></i>&nbsp;Delete</a>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            <% }); %>
                            <% if(course.assignments.length === 0) { %>
                                <tr>
                                    <td>No Assignments</td>
                                    <td></td>
                                    <td></td>
                                    <td></td>
                                    <td></td>
                                    <td></td>
                                    <td></td>
                                </tr>
                            <% } %>
                            </tbody>
                        </table>
                    </div>
                </div><br>
                <div id="confirmDeleteCourse" class="modal fade">
                    <div class="modal-dialog modal-login">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h4 class="modal-title">Delete Course</h4>
                                <button type="button" class="close" data-dismiss="modal"
                                        aria-hidden="true">&times;
                                </button>
                            </div>
                            <div class="modal-body d-flex justify-content-between align-items-center">
                                <span>
                                    Delete course <strong><%= course.name %>.<%= course.section %></strong>?
                                </span>
                                <span>
                                <a class="btn btn-primary text-white" data-dismiss="modal">Cancel</a>
                                <a class="btn btn-danger" href="/courses/<%= course._id %>/delete"><i
                                            class="fas fa-trash-alt"></i> Delete Course</a>
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</div>
</body>
<div id="newClass" class="modal fade">
    <div class="modal-dialog modal-login">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">New Course</h4>
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
            </div>
            <form action="/courses" method="post">
                <div class="modal-body">
                    <div class="form-group">
                        <label>Name</label>
                        <input type="text" class="form-control" name="name" id="name" placeholder="Name">
                    </div>
                    <div class="form-group">
                        <label>Section</label>
                        <input type="number" class="form-control" name="section" id="section" placeholder="0">
                    </div>
                </div>
                <div class="modal-footer">
                    <input type="submit" class="btn btn-primary pull-right" value="Create Course">
                </div>
            </form>
        </div>
    </div>
</div>

<%- include("partials/footer.ejs") %>
</html>

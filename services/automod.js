let Topic = require('../models/topic');

let blacklist = JSON.parse(require("../blacklist.json"));

const verifyBody = function(body){
  var strBody = body.split(" ");
  let c = false;
  strBody.forEach(function(strB){
  
  });
}
